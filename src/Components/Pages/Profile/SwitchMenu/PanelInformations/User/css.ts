import styled from "styled-components";
import { colors } from "StyledHelpers/Colors";
import * as con from "StyledHelpers/Controls";

export const ContentS = styled(con.TriangleConent)`
  background-color: ${colors.Bg2};
  margin: 10px 0;
  padding: 0 5px;
`;

export const LeftS = styled(con.TriangleConentLeft)``;

export const RightS = styled(con.TriangleConentRight)``;
